# 102-Reading_07

*What is control flow?*

**The order in which your code runs from top to bottom. js runs line by line, top to bottom but sometimes (if and else) it isnt linear. It is more efficent to create functions, back ups, or other control flow items.**

** Buttons are based on user input, then js runs. Or in an automatic case-prompt will run automatically. Based on user input is how you would chose certain function or code. Its programmed in case it is used.** *coding isnt always needed or run*
**Sometimes you jump around based on the conditions. IE: alt route)**

*What is a JavaScript function?*

**It is a resusable block of code. It helps organize code, avoids repeating yourself, and makes fixes easier later**

*What does it mean to invoke - or call - a function?*

**running the code, to make it run. Calling the function turns the switch on. If you don't call it the function never runs**


*What are the parenthesis () for when you define a function?*

**Storing parameters (place holders) there. Parameter when youre defining function
use arguements when youre calling the function. they mean the
same thing but are used deferrently arguements are the replacement for valeues - numbers strings or booleans as an example**

**pass in a value** 

**your function doesnt need any data to pass on
you must have the function name to call once you have previously made the function**
